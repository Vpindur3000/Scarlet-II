# Local network directory

This directory intentionally contains no weights.

Configure paths through UCI, for example:

- `setoption name BerserkNNUEFile value /path/to/compatible.nn`
- `setoption name LeelaWeightsFile value /path/to/compatible.pb`
